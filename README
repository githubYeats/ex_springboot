#### lombok插件
主要作用：
    1）简化pojo的编写             pojo: Plain Ordinary Java Object
    2）为Bean对象提供日志功能

项目中引入lombok插件后，在Bean上使用下面注解：
    @Data       自动提供getter和setter、hashCode、equals、toString等方法
    @Getter     自动提供getter方法
    @Setter     自动提供setter方法
    @Slf4j      自动在Bean中提供log变量，其实用的是slf4j的日志功能。
        简单日志门面（Simple Logging Facade for Java）

实现步骤：
    1.为项目配置lombok插件
        1）引入lombok依赖
        2）IDEA安装lombok插件
            不安装插件，在IDEA中使用lombok的注解虽然编译能通过，但是源码会报错。所以为了让IDEA更好地辨别lombok注解，才安装插件。
    2.在springboot_test数据库中创建一张表tb_user
    3.编写pojo类