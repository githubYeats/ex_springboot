#### SpringBoot整合Hikari连接池

SpringBoot本身自带有一个数据库连接池：HikariCP
在引入jdbc启动器的时候，SpringBoot已经自动帮我们引入了这个连接池。

需要做的是：进行配置
    application.yml中配置Hikari连接池
        # 加载并激活其他yml配置文件
        spring:
          profiles:
            active: baidu, google
          # ----HikariCP连接池配置----
          datasource:
            driverClassName: com.mysql.jdbc.Driver
            url: jdbc:mysql://127.0.0.1:3306/springboot_test
            username: root
            password: root
    把 JdbcConfig 类中的关于Druid连接池的配置删除或注释掉
        直接将JdbcConfig类删除掉

配置好后，启动项目，访问controller，断点测试，查看dataSource对象