#### SpringBoot整合Junit

实现步骤：
    1.引入SpringBoot单元测试启动器
        <!--SpringBoot的单元测试启动器-->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
        </dependency>
    2.项目测试包中编写测试类
        SpringBoot的测试要加两个注解：
            @RunWith(SpringRunner.class) //指定新的运行器，由它来创建IoC容器
            @SpringBootTest // 指定SpringBoot测试类